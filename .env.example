# ── LŌCUS Environment Variables ──────────────────────────────
# .env.example — 실제 값은 .env.local에 저장 (git 제외)

# ── API ──────────────────────────────────────────────────────
PORT=4000
WEB_URL=http://localhost:3000

# ── AI Engine ────────────────────────────────────────────────
# Phase 1: Claude API (프로토타입)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Phase 2: KoBERT 모델 경로 (학습 후 설정)
# KOBERT_MODEL_PATH=./models/locus-kobert-v1

# ── Database ─────────────────────────────────────────────────
# 개발: SQLite
DB_PATH=./locus.db

# 프로덕션: PostgreSQL (추후)
# DATABASE_URL=postgresql://user:password@host:5432/locus

# ── Next.js ──────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:4000
